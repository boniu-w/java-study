# 1. org.mapstruct.Mapper; 日期时区问题

```java
@Mapper(componentModel = MappingConstants.ComponentModel.SPRING)
public interface GlobalDateMapper {
    default LocalDateTime dateToLocalDateTime(java.util.Date date) {
        if (date == null) {
            return null;
        }
        return LocalDateTime.ofInstant(date.toInstant(), ZoneId.systemDefault());
    }
}


@Mapper(componentModel = "spring", uses = GlobalDateMapper.class)
public interface SpMflDataTblMapping {
    SpMflDataTblDTO toDtoFromExcel(SpIiMflDataTblExcel excel);

    List<SpMflDataTblDTO> toDtoListFromExcel(List<SpIiMflDataTblExcel> excels);    
}
```

